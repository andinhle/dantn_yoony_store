import queryString from 'query-string';
const PaymentCheckVnpay = () => {
    const parsed = queryString.parse(location.search);
    console.log(parsed)

  return (
    <div>
        
    </div>
  )
}

export default PaymentCheckVnpay